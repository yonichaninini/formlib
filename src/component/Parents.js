import React from "react";
import Chilren from "./Chilren";

const Parents = ({ initialData, onSubmit }) => {
  return <Chilren initialData={initialData} onSubmit={onSubmit} />;
};

export default Parents;
